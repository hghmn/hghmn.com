# hghmn.github.io
Github Page for hghmn

Build: `docker run -it --rm --volume="$PWD:/srv/jekyll" jekyll/jekyll:pages jekyll build`
Build: `docker run -it --rm --volume="$PWD:/srv/jekyll" jekyll/jekyll:pages jekyll build`
